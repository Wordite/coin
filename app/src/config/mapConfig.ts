// Map Configuration
// Adjust these values to position the map texture

export const MAP_CONFIG = {
  // Position and size of the full map texture (pful1.png)
  texture: {
    x: 0.3,       // X offset - move right (+) or left (-)
    y: -0.2,      // Y offset - move up (+) or down (-)
    width: 12,    // Width of the texture plane
    height: 7,    // Height of the texture plane
    z: 0.01       // Z position (layer depth)
  }
}
